query getProjects($deployed: Boolean, $network: String) {
  projects(deployed: $deployed, network: $network) {
    id
    image
    name
    slug
    icon
    description
    repo_url
    website_url
    deployed
  }
}
